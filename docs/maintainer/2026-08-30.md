# Weekly maintainer report - 2026-08-30

First report of its kind, and it opens with the thing that shapes every section
below: **the brief this session ran under is written for a live project, and this
project is not live.** The repository has no git remote, `spelingbee/restored`
does not resolve on GitHub, nothing is tagged and nothing is published. That is
not a failure - it is stop points 1, 3, 4 and 6 in CLAUDE.md, all still closed,
all deliberately a human's. But it means the pipeline this session exists to keep
healthy does not exist yet. Everything below is the brief run honestly against
that state, with the evidence inline.

```text
$ git remote -v
(nothing)
$ gh repo view spelingbee/restored --json name,visibility
GraphQL: Could not resolve to a Repository with the name 'spelingbee/restored'. (repository)
```

## 1. KPI

| metric | this week | last week |
|---|---|---|
| distinct external authors of merged PRs, trailing 365 days | 0 | no prior report |
| merged PRs this week | 0 - no repository | - |
| open PRs, and their age | 0 - no repository | - |
| open `recipe` / `recipe-broken` / `help wanted` issues | 0 - no repository. 39 backlog (38 from the session 4 reviews, plus MNT-19 below) + 50 recipes-wanted issues are drafted and waiting on launch | - |
| weekly `recipe-health` run | never run - no repository. Local substitute below | - |

```text
$ ./scripts/contributors.sh
no git remotes found
$ ./scripts/contributors.sh --repo spelingbee/restored
GraphQL: Could not resolve to a Repository with the name 'spelingbee/restored'. (repository)
repo:   spelingbee/restored
since:  2025-08-30

No external contributor has had a pull request merged yet.
```

That second run is also a finding: the script prints the GraphQL error and then
reports zero with exit 0. After launch, an API failure would read as "0
contributors" on a dashboard. Recorded as **MNT-19** in
`docs/review/backlog.md`, not fixed here - it is exactly the size of thing the
backlog exists to hand a contributor.

## 2. Pull requests

There are none, because there is nowhere for one to exist. The P0 rule of this
brief - respond to anything unanswered for more than 24 hours - is vacuously
satisfied, and will be every week until stop point 4 is opened.

Nothing marked `ready-to-merge`. Nothing labelled at all; the labels themselves
have not been created (`scripts/labels.sh` is still in its dry-run state).

## 3. Issues

None exist, none were filed (filing them pre-launch is impossible, and doing so
at launch is scripted: `scripts/backlog-issues.sh --apply` and
`scripts/recipes-wanted.sh` in that order, per `docs/review/backlog.md`).

## 4. Registry hygiene

All verified on this commit (53804b7), today.

Generated files are current - the generators were re-run and produced no diff:

```text
$ go run ./tools/gen recipe-spec > docs/recipe-spec.md
$ go run ./tools/gen recipes-index > recipes/README.md
$ go run ./tools/gen readme-table
$ git diff --stat -- docs/recipe-spec.md recipes/README.md README.md
(nothing)
```

All twenty recipe directories (nineteen recipes plus TEMPLATE) validate strictly:

```text
$ ./bin/restored recipe validate ./recipes/*/ --strict
ok       ./recipes/TEMPLATE/
... 20 lines, all ok ...
$ echo $?
0
```

`.all-contributorsrc` has an empty contributors list, which matches the zero
merged external authors exactly. Nothing to add.

Recipes changed this week that CI could not cover: all nineteen - CI has never
run. The full-registry evidence is in PROGRESS.md session 5, recorded the same
day on the same recipe content (`recipes/` last changed in cb3feba). As a fresh
weekly health signal, the two fastest recipes were round-tripped again today
rather than re-running the 20-minute registry for a zero-delta week:

```text
$ ./bin/restored recipe test ./recipes/vaultwarden ./recipes/freshrss
  PASS   vaultwarden in 16.5s
  PASS   freshrss in 22.4s
  2 recipes: 2 passed, 0 failed, 0 errored, in 38.9s
$ docker ps -aq --filter "label=com.restored.run" | wc -l
0
```

The suite, uncached, plus lint:

```text
$ go test ./... -count=1
ok      github.com/spelingbee/restored/internal/check   1.863s
... 12 packages, all ok ...
$ gofmt -l .
(nothing)
$ go vet ./... && go vet -tags integration ./...
(nothing)
$ ./scripts/lint-english.sh
lint-english: ok
```

## 5. Drill and content

**Queue state: the trigger is not met.** The brief adds five apps when fewer
than three untested ones remain; `docs/drill/SKIPPED.md` lists at least seven
feasible, untested applications already queued - Stirling-PDF (attempted,
stopped at its unscriptable login), photoprism, appsmith, plausible, karakeep,
ArchiveBox, linkwarden - plus the 13-19k-star band of single-container SQLite
apps. Nothing was added and no drill leg was run: fourteen tested applications
against a target of fifteen, with the queue full. The next drill leg is a
decision about session time, not about the queue.

**Found in the working tree, live:** a ConvertX drill leg is being written by
a concurrent session while this report is being finished - `docs/drill/convertx/`
(its `compose.yaml` at 20:40, result files still landing at 20:52) and an
untracked `recipes/convertx/`, whose first `recipe test` run failed two checks
and whose second passed all six. None of it is tracked, none of it is committed
by this session, and this report makes no claim about its verdict - that
belongs to the session running it, which was messaged directly so the two do
not collide in PROGRESS.md. One consequence for the evidence in section 4: the
20-line `recipe validate` output was captured before the untracked
twenty-first directory appeared; at HEAD the registry is still nineteen recipes
plus TEMPLATE, which is what the committed claim describes.

The drafted post is at the end of this report.

## 6. Decisions that need a human

1. **Launch, or park the weekly cadence.** This is the only real decision on the
   table. Every section above is empty for the same reason, and every weekly
   report will look like this one until the repository is public.
   `docs/release-checklist.md` is the ordered list; its first items are
   repository settings no session can reach. If launch is not imminent, this
   brief should be scheduled to start after stop point 4, not before - the
   session it replaces (internal/config, PROGRESS.md "Next steps") is currently
   the better use of the time.
2. **The CONTRIBUTING.md review promise** ("first response within 24 hours,
   merged within 48 when CI is green") becomes binding the moment the
   repository is public, and this report is the mechanism that would keep it -
   weekly is not a 24-hour cadence. Confirm who or what answers inside 24 hours
   before launch. (Open question 8 in PROGRESS.md, restated because this brief
   is where it lands.)
3. **MNT-19**, above, if a query-failure-reads-as-zero KPI bothers anyone before
   a contributor picks it up.

## Fake-growth check

Nothing to flag, in the strongest possible sense: there are no contributions,
no accounts, no activity of any kind to be fake. The check has nothing to run
against until launch.

## Drafted post - NOT posted

For a Mastodon/X post or a Discussions update, once posting is a thing this
project does (stop point 3). 118 words by `wc -w`. No external author exists yet to
credit; the drill is the project's own work, so no credit line is invented.

```text
This week's drill: take fourteen of the most popular self-hosted apps, follow
each one's official backup guidance as written - half turn out to have no
backup page - then restore the result and check what comes back. Nine restored
whole, two partial, two failed outright, one was not decidable on our host.
The sharpest case is Navidrome: its documented "backup restore" command prints
"Restore complete" in six milliseconds and leaves the instance empty. The
users and playlists are in the backup file; they just never come back. Every
case is written up with exact commands and real output, and each app now has a
tested restore recipe. The database file alone is rarely all of your data.
```
