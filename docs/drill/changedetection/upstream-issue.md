# changedetection.io - no issue drafted

**Status: nothing to file.**

The backup the application produces restores the watches and their history, by the
procedure the project's own wiki page describes, with no missing step. There is no gap
to report and nothing has been filed.

One thing was noted in [result.md](result.md) as a compliment rather than a request: the
wiki page opens with

> Important first is to stop the changedetection.io instance, as it will be monitoring
> and writing to the disk.

which is the sentence two other applications in this drill lose data for want of.

If anything were worth suggesting, it would be small: the Backups page inside the
application says what a backup is but not where it lands or that it is written into
`/datastore` itself, so a person whose disk is full has one more thing to know. That is
too thin to open an issue about.
