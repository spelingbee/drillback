restored 0.1.0-dev · recipe memos · run 5zsg7sbn

  source     restic  C:/Users/kadyr/AppData/Local/Temp/restored-drill/memos/restic-db-only
  snapshot   bba3a840  2026-08-30 11:39:12  host=drill  tags=[drill]
  inputs     data  /var/opt/memos                  4.0 KiB  1 file
             db    /var/opt/memos/memos_prod.db    4.0 KiB  1 file

  restore    ok          1.6s   2 inputs
  compose    ok          1.0s   1 service
  load db    ok         0.02s   db: sqlite integrity_check, 0 stderr lines
  ready      ok         0.49s   Memos answers /healthz

  CHECKS
  ✔  healthz        The server answers its health endpoint               0.58s
  ✔  db-integrity   The SQLite database passes PRAGMA integrity_check    0.01s
                    → ok
  ✘  users-present  At least one account survived the restore            0.01s
                      query   SELECT count(*) FROM user;
                      expect  scalar_int_min: 1
                      got     0
  ✘  memos-present  The memos survived the restore                       0.02s
                      query   SELECT count(*) FROM memo;
                      expect  scalar_int_min: 1
                      got     0
  ✘  signin-works   The restored account can still sign in               0.54s
                      expect  status: 200
                      got     status: 400
                      expect  body_matches: "accessToken"
                      got     {"code":3, "message":"unmatched username and password", "details":[]}

  RESTORE UNUSABLE  2/5 checks  ·  total 5.0s  ·  teardown ok

  LIKELY CAUSE
    The application's tables are there, but they are empty

    Every table the checks read exists and holds nothing. Two causes produce
    exactly this. Either the dump was taken from the wrong database, or with
    `pg_dump --schema-only`, or narrowed with `--table`, so it carried a
    schema and none of the rows. Or the dump carried nothing at all and the
    application rebuilt an empty schema for itself on start, which is what
    an application with automatic migrations does the moment it meets an
    empty database. Compare the size of the dump with the size of the data
    directory in the report above: a forge with repositories on disk and a
    half-kilobyte dump is not a backup.

      grep -c 'INSERT INTO' /var/opt/memos/memos_prod.db
      ls -l /var/opt/memos/memos_prod.db
                                                      (hint: db/tables-empty)

  Service logs from the failure window are in the JSON report (--report).
  Re-run with --keep to keep the stack up and poke at it yourself.
